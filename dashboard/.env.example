# Supabase project (anon/public key only — safe for the browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Base URL of the platform API (platform/src/server.ts), no trailing slash
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
